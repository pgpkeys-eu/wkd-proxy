services:
  wkd-proxy:
    build:
      context: .
    ports:
    - "8080:8080"
    restart: always
    volumes:
    - ./etc:/wkd-proxy/etc
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
